export default function View() {
    return (
        <div className='viewPage'>
            <h1>View the model</h1>
            <p>Will render the viewer to see the model in space. Linked from clicking on a pin on the map.</p>
        </div>
    );
}